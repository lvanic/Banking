<UserControl x:Class="Banking.Views.Options"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Banking.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" MinWidth="400">
        <Menu VerticalAlignment="Top" Background="{x:Null}" Foreground="Black" >
            <MenuItem x:Name="menuTheme" Header="{DynamicResource m_OptionThemes}" />
        </Menu>
        <Menu VerticalAlignment="Top" Background="{x:Null}" Foreground="Black" Margin="0,50,0,0">
            <MenuItem x:Name="menuLanguage" Header="{DynamicResource m_OptionLanguage}" />
        </Menu>
    </Grid>
</UserControl>
